# 更新日志

## [Unreleased]

### 安全
- 移除硬编码的默认密码；全新部署改为通过 `scripts/init_admin.py` 显式创建管理员（空库下注册接口本身需要管理员权限，无法自助注册）
- 资料下载端点补齐登录鉴权，未登录不再可枚举下载
- Token 有效期调整：access 2 小时 → 7 天，refresh 7 天 → 30 天（单机自用场景）
- 完善 .gitignore，确保 data/ 目录和敏感文件不被提交；补充排除验证截图与本地沙箱脚本

### 新增
- `scripts/init_admin.py`：一键创建 / 重置管理员账号，解决全新部署无账号可登录的死锁
- 后端模块：materials / notifications / search / system
- 全局搜索、通知中心

### 修复
- 后端由单线程改为多线程，并发请求不再因一个慢请求全部排队超时
- 前端 401 自动刷新从未生效（错误码匹配失效），token 过期后直接掉登录
- 登录 / 刷新响应缺 `expires_in`，导致前端推算的过期时间被存成 NaN
- 后端 pytest 套件收集阶段即报错（用例停留在旧扁平权限模型），已对齐「角色 × 模块 × 动作」矩阵，79 项全通过

### 变更
- 存储收敛为 SQLite 单文件（`data/workbench.db`），移除 JSON 同步快照
- 编辑知识页简化：去掉重复按钮组与分区标题，关联业务改为折叠
- 清理旧单体 `server.py` 中前端从不调用的遗留端点与死代码（约 1130 行）

### 文档
- 重写 README：修正启动命令、端口、账号初始化、token 有效期与架构描述
- 更新 README 中的默认账号说明

## [2.3.0] - 2026-04-23

### 新增
- 首页仪表盘数据总览
- 项目管理模块
- 选手管理模块（含资料完整性检测、批量导入）
- 机构管理模块
- 财务管理模块
- 知识库模块
- 归档管理模块（含 Windows 系统原生图标）
- 素材库模块
- 模板管理模块
- 资料配置模块
- 检查清单模块
- 用户管理模块（RBAC 权限）
- 操作日志模块
- 系统设置（亮色/深色主题切换）
- JWT 认证
- Electron 桌面应用支持
